第一题：
用两个归并排序；先将x顺序排序，再统计y的顺序对数即为输出结果。
第二题：
创建一个双向循环链表，再写一个插入函数insert，用next指针每隔m项插入一个新的节点，最后以最后一个插入的节点为第一个，用front指针输出所有数据。
第三题：
用数组实现一个栈，并写出相应的full，empty，top，pop，push函数，主函数中主要判定代码为：
for(int i=0;i<n;i++)
	{
		while(empty() || (size>0 && b[i]!=top()))
		{
			if(t>n || full())
			{
				puts("No");
				return 0;
			}
			push(t++);
		}
		pop();	
	}
若运行完此循环程序没有结束说明有可行解，输出相应的可行解即可。